# FastAPI framework for building the proxy server
fastapi==0.135.2
# ASGI server for running the FastAPI application
uvicorn==0.42.0
# For loading environment variables from a .env file
python-dotenv==1.0.1

# Installs the local rotator_library in editable mode
-e src/rotator_library

# A library for calling LLM APIs with a consistent format
# Pinned to 1.82.0 — versions 1.82.7 and 1.82.8 were compromised (credential theft via litellm_init.pth)
litellm==1.82.0

filelock==3.16.1
httpx==0.28.1
aiofiles==24.1.0
aiohttp==3.11.11

colorlog==6.9.0

rich==13.9.4

# GUI for model filter configuration
customtkinter==5.2.2

# For building the executable
pyinstaller==6.11.1
