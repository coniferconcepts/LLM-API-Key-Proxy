# FastAPI framework for building the proxy server
fastapi==0.140.13
# ASGI server for running the FastAPI application
uvicorn==0.48.0
# For loading environment variables from a .env file.
# LiteLLM 1.83.14 requires python-dotenv 1.2.2.
python-dotenv==1.2.2

# Installs the local rotator_library in editable mode
-e src/rotator_library

# A library for calling LLM APIs with a consistent format.
# Avoid 1.82.7 and 1.82.8: those releases were compromised via litellm_init.pth.
# GH#17 P0: floor LiteLLM >=1.84.0 (advisories GHSA-4xpc-pv4p-pm3w, GHSA-7488-6r32-c95q).
# 1.96.2 is the vendor-EOL hop onto a still-supported minor. Not GHSA-3cv6 closure
# (that advisory is LiteLLM Proxy is_request_body_safe; we do not run Proxy).
litellm==1.96.2

filelock==3.32.0
httpx==0.28.1
aiofiles==24.1.0
# LiteLLM 1.96.x requires aiohttp>=3.14.2,<4; GH#17 floor is 3.14.1+ (use 3.14.3).
aiohttp==3.14.3

colorlog==6.9.0

rich==13.9.4

# GUI for model filter configuration
customtkinter==5.2.2

# For building the executable
pyinstaller==6.11.1
