[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100

[tool.ruff.lint.per-file-ignores]
# main.py deliberately defers heavy imports until after argument and environment setup.
"src/proxy_app/main.py" = ["E402"]
